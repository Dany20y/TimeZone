@{
    Layout = "~/Views/Shared/NoHero.cshtml";
    ViewBag.Title = "Login";
}

@using System.Web.Optimization
@model Time_Zone.Models.userLogin

<section class="login_part section_padding">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6 col-md-6">
                <div class="login_part_text text-center">
                    <div class="login_part_text_iner">
                        <h2>New to our Shop?</h2>
                        <p>There are advances being made in science and technology everyday, and a good example of this is the</p>
                        <a href="@Url.Action("Register", "Account")" class="btn_3">Create an Account</a> <!-- Dynamic URL generation for MVC actions -->
                    </div>
                    }
                <form method="post" class="needs-validation" id="registrationForm" novalidate>
                    @using (Html.BeginForm("Login", "Home", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <!-- Email input -->
                        <div class="form-outline mb-4">
                            @Html.TextBoxFor(m => m.Username, new { @class = "form-control", id = "email" })
                            @Html.LabelFor(m => m.Username, "Email", new { @class = "form-label" })
                            @Html.ValidationMessageFor(m => m.Username)
                        </div>

                        <!-- Password input -->
                        <div class="form-outline mb-4">
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control", id = "password" })
                            @Html.LabelFor(m => m.Password, "Password", new { @class = "form-label" })
                            @Html.ValidationMessageFor(m => m.Password)
                        </div>

                        <!-- Submit button -->
                        <button type="submit" class="btn btn-primary btn-block mb-4">
                            Sign in
                        </button>

                </div>
            </div>
            <div class="col-lg-6 col-md-6">
                <div class="login_part_form">
                    <div class="login_part_form_iner">
                        <h3>Welcome Back! <br> Please Sign in now</h3>
                        @using (Html.BeginForm("Home", "Login", FormMethod.Post, new { id = "LoginForm", @class = "row contact_form", novalidate = "novalidate" }))
                        {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })  <!-- Validation summary to display all form errors -->
                        <div class="col-md-12 form-group p_star">
                            @Html.LabelFor(m => m.Credential, new { @class = "control-label" })
                            @Html.TextBoxFor(m => m.Credential, new
                            {
                                @class = "form-control",
                                placeholder = "example@gmail.com",
                                title = "Please enter your username"
                            })
                            <span class="help-block small">Your unique username to app</span>
                            @Html.ValidationMessageFor(m => m.Credential, "", new { @class = "text-danger" }) <!-- Individual field validation message -->
                        </div>
                        <div class="col-md-12 form-group p_star">
                            @Html.LabelFor(m => m.Password, new { @class = "control-label" })
                            @Html.PasswordFor(m => m.Password, new
                            {
                                @class = "form-control",
                                placeholder = "******",
                                title = "Please enter your password"
                            })
                            <span class="help-block small">Your strong password</span>
                            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" }) <!-- Individual field validation message -->
                        </div>
                        <div class="col-md-12 form-group">
                            <div class="creat_account d-flex align-items-center">
                                <input type="checkbox" id="f-option" name="selector">
                                <label for="f-option">Remember me</label>
                            </div>
                            <button type="submit" class="btn_3">log in</button>
                            <a class="lost_pass" href="#">forget password?</a>
                        </div>
>>>>>>> 7600afd34e4ddb84e43620e9d27ed775f185718d
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
